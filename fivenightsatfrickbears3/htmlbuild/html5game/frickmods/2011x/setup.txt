WHERE TO PUT THIS FOLDER:

Look for "AppData/Local/Frickbears3". Go inside, and look for a folder titled "addons". If there's not already a folder titled "addons" in the Frickbears3 folder, create one and put this character's folder inside it.